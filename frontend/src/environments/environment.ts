// Entorno de desarrollo: la API corre localmente (ver README del backend,
// Actividad 8: `mvn spring-boot:run`, puerto 8080 por defecto).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};

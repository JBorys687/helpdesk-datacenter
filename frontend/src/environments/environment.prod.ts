// Entorno de producción: URL pública del backend desplegado (Actividad 9,
// requisito "Despliegue en la Nube"). Se reemplaza tras desplegar en Render.
export const environment = {
  production: true,
  apiUrl: 'https://helpdesk-datacenter-api.onrender.com',
};
